package hospital.management.system;import net.proteanit.sql.DbUtils;import javax.swing.*;import javax.swing.border.LineBorder;import java.awt.*;import java.awt.event.*;import java.sql.ResultSet;public class Employee_info extends JFrame {    Employee_info() {        setTitle("Employee Information");        setUndecorated(true);        setSize(1000, 600);        setLocationRelativeTo(null);        setLayout(null);        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);        JPanel panel = new JPanel();        panel.setBounds(5, 5, 990, 590);        panel.setBackground(new Color(109, 164, 170));        panel.setLayout(null);        panel.setBorder(new LineBorder(Color.DARK_GRAY, 2, true));        add(panel);        // Labels for table headers        String[] headers = {"Name", "Age", "Phone Number", "Salary", "Gmail", "Aadhar Number"};        int[] positions = {41, 180, 350, 550, 730, 830};        for (int i = 0; i < headers.length; i++) {            JLabel label = new JLabel(headers[i]);            label.setBounds(positions[i], 9, 150, 20);            label.setFont(new Font("Tahoma", Font.BOLD, 14));            label.setForeground(Color.WHITE);        }        // JTable setup inside a JScrollPane        JTable table = new JTable();        table.setFont(new Font("Tahoma", Font.PLAIN, 13));        table.setRowHeight(25);        table.setBackground(new Color(225, 240, 240));        table.getTableHeader().setFont(new Font("Tahoma", Font.BOLD, 14));        JScrollPane scrollPane = new JScrollPane(table);        scrollPane.setBounds(10, 34, 970, 450);        scrollPane.setBorder(new LineBorder(Color.GRAY, 1, true));        panel.add(scrollPane);        // Fetch and populate table data        try {            conn c = new conn();            String q = "SELECT * FROM EMP_INFO";            ResultSet resultSet = c.statement.executeQuery(q);            table.setModel(DbUtils.resultSetToTableModel(resultSet));        } catch (Exception e) {            e.printStackTrace();        }        // BACK button with hover effect        JButton backButton = new JButton("BACK");        backButton.setBounds(420, 510, 140, 35);        backButton.setFont(new Font("Tahoma", Font.BOLD, 14));        backButton.setBackground(Color.BLACK);        backButton.setForeground(Color.WHITE);        backButton.setFocusPainted(false);        backButton.setCursor(Cursor.getPredefinedCursor(Cursor.HAND_CURSOR));        panel.add(backButton);        // Hover effect for BACK button        backButton.addMouseListener(new MouseAdapter() {            public void mouseEntered(MouseEvent e) {                backButton.setBackground(Color.WHITE);                backButton.setForeground(Color.BLACK);                backButton.setBorder(new LineBorder(Color.BLACK));            }            public void mouseExited(MouseEvent e) {                backButton.setBackground(Color.BLACK);                backButton.setForeground(Color.WHITE);            }        });        // Close window on BACK button click        backButton.addActionListener(e -> setVisible(false));        setVisible(true);    }    public static void main(String[] args) {        new Employee_info();    }}