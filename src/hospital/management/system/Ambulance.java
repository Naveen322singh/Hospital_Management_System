package hospital.management.system;import net.proteanit.sql.DbUtils;import javax.swing.*;import javax.swing.table.JTableHeader;import java.awt.*;import java.awt.event.*;import java.sql.ResultSet;public class Ambulance extends JFrame {    public Ambulance() {        setTitle("Ambulance Details");        setLayout(null);        getContentPane().setBackground(new Color(30, 42, 56)); // Dark blue-gray        setSize(900, 600);        setLocation(300, 200);        setUndecorated(true);        Color panelColor = new Color(46, 139, 139);  // Teal        Color headerColor = new Color(0, 77, 64);    // Dark teal        Color buttonColor = new Color(255, 167, 38); // Orange        Color hoverColor = new Color(251, 140, 0);   // Hover orange        Font fontBold = new Font("Segoe UI", Font.BOLD, 14);        JPanel panel = new JPanel(null);        panel.setBounds(5, 5, 890, 590);        panel.setBackground(panelColor);        add(panel);        // Table setup        JTable table = new JTable();        table.setBounds(10, 40, 870, 450);        table.setBackground(panelColor);        table.setForeground(Color.WHITE);        table.setFont(new Font("Segoe UI", Font.PLAIN, 13));        table.setRowHeight(25);        // Scroll pane to hold table        JScrollPane scrollPane = new JScrollPane(table);        scrollPane.setBounds(10, 40, 870, 450);        scrollPane.getViewport().setBackground(panelColor);        scrollPane.setBorder(BorderFactory.createEmptyBorder());        panel.add(scrollPane);        // Table header styling        JTableHeader header = table.getTableHeader();        header.setBackground(headerColor);        header.setForeground(Color.WHITE);        header.setFont(fontBold);        // Load data from database        try {            conn c = new conn();            ResultSet rs = c.statement.executeQuery("SELECT * FROM Ambulance");            table.setModel(DbUtils.resultSetToTableModel(rs));        } catch (Exception e) {            e.printStackTrace();        }        // Column labels        String[] headers = {"Name", "Gender", "Car name", "Available", "Location"};        int[] xPos = {15, 180, 366, 520, 750};        for (int i = 0; i < headers.length; i++) {            JLabel label = new JLabel(headers[i]);            label.setBounds(xPos[i], 11, 120, 20);            label.setFont(fontBold);            label.setForeground(Color.WHITE);        }        // BACK Button        JButton backBtn = new JButton("BACK");        backBtn.setBounds(385, 510, 120, 35);        backBtn.setBackground(buttonColor);        backBtn.setForeground(Color.BLACK);        backBtn.setFont(new Font("Segoe UI", Font.BOLD, 14));        backBtn.setFocusPainted(false);        backBtn.setCursor(new Cursor(Cursor.HAND_CURSOR));        backBtn.setBorder(BorderFactory.createLineBorder(Color.DARK_GRAY));        // Hover effect        addHoverEffect(backBtn, buttonColor, hoverColor);        backBtn.addActionListener(e -> setVisible(false));        panel.add(backBtn);        setVisible(true);    }    private void addHoverEffect(JButton button, Color normal, Color hover) {        button.addMouseListener(new MouseAdapter() {            public void mouseEntered(MouseEvent e) {                button.setBackground(hover);            }            public void mouseExited(MouseEvent e) {                button.setBackground(normal);            }        });    }    public static void main(String[] args) {        new Ambulance();    }}