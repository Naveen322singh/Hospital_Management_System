package hospital.management.system;import net.proteanit.sql.DbUtils;import javax.swing.*;import java.awt.*;import java.awt.event.*;import java.sql.ResultSet;public class SearchRoom extends JFrame {    Choice choice;    JTable table;    SearchRoom() {        setTitle("Search Room");        setUndecorated(true);        setSize(700, 500);        setLayout(null);        setLocationRelativeTo(null);        JPanel panel = new JPanel();        panel.setBounds(5, 5, 690, 490);        panel.setBackground(new Color(90, 156, 163));        panel.setLayout(null);        add(panel);        JLabel title = new JLabel("Search For Room");        title.setBounds(240, 15, 250, 30);        title.setForeground(Color.WHITE);        title.setFont(new Font("Tahoma", Font.BOLD, 22));        panel.add(title);        JLabel status = new JLabel("Status:");        status.setBounds(70, 70, 80, 20);        status.setForeground(Color.WHITE);        status.setFont(new Font("Tahoma", Font.BOLD, 14));        panel.add(status);        choice = new Choice();        choice.setBounds(170, 70, 150, 25);        choice.setFont(new Font("Tahoma", Font.PLAIN, 13));        choice.add("Available");        choice.add("Occupied");        panel.add(choice);        // Column headers        String[] headers = {"Room Number", "Availability", "Price", "Bed Type"};        int[] xBounds = {30, 180, 380, 500};        for (int i = 0; i < headers.length; i++) {            JLabel header = new JLabel(headers[i]);            header.setBounds(xBounds[i], 140, 150, 20);            header.setForeground(Color.WHITE);            header.setFont(new Font("Tahoma", Font.BOLD, 14));            panel.add(header);        }        // Table in scroll pane        table = new JTable();        table.setFont(new Font("Tahoma", Font.PLAIN, 13));        table.setRowHeight(25);        table.setGridColor(Color.DARK_GRAY);        table.setShowGrid(true);        table.setBackground(new Color(226, 241, 241));        JScrollPane scrollPane = new JScrollPane(table);        scrollPane.setBounds(10, 160, 670, 230);        panel.add(scrollPane);        // Load all data initially        try {            conn c = new conn();            ResultSet rs = c.statement.executeQuery("SELECT * FROM room");            table.setModel(DbUtils.resultSetToTableModel(rs));        } catch (Exception e) {            e.printStackTrace();        }        // Buttons        JButton searchBtn = createStyledButton("Search", 180, 420);        panel.add(searchBtn);        searchBtn.addActionListener(e -> {            String query = "SELECT * FROM Room WHERE Availability = '" + choice.getSelectedItem() + "'";            try {                conn c = new conn();                ResultSet rs = c.statement.executeQuery(query);                table.setModel(DbUtils.resultSetToTableModel(rs));            } catch (Exception ex) {                ex.printStackTrace();            }        });        JButton backBtn = createStyledButton("Back", 360, 420);        panel.add(backBtn);        backBtn.addActionListener(e -> setVisible(false));        setVisible(true);    }    private JButton createStyledButton(String text, int x, int y) {        JButton button = new JButton(text);        button.setBounds(x, y, 120, 30);        button.setBackground(Color.BLACK);        button.setForeground(Color.WHITE);        button.setFont(new Font("Tahoma", Font.BOLD, 13));        button.setFocusPainted(false);        button.setCursor(Cursor.getPredefinedCursor(Cursor.HAND_CURSOR));        button.addMouseListener(new MouseAdapter() {            public void mouseEntered(MouseEvent e) {                button.setBackground(Color.WHITE);                button.setForeground(Color.BLACK);                button.setBorder(BorderFactory.createLineBorder(Color.BLACK));            }            public void mouseExited(MouseEvent e) {                button.setBackground(Color.BLACK);                button.setForeground(Color.WHITE);            }        });        return button;    }    public static void main(String[] args) {        new SearchRoom();    }}